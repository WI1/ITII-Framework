import { Routes } from '@angular/router';
import { HistoryComponent } from './page/history/history.component';

export const HISTORY_ROUTES: Routes = [
  { path: '', component: HistoryComponent },
];

export default HISTORY_ROUTES;
