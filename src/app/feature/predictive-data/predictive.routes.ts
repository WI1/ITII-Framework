import { Routes } from '@angular/router';

import { PredictiveComponent } from './page/predictive/predictive.component';

export const PREDICTIVE_ROUTES: Routes = [
  { path: '', component: PredictiveComponent },
];

export default PREDICTIVE_ROUTES;
