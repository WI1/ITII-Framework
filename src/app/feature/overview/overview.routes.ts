import { Routes } from '@angular/router';
import { OverviewComponent } from './page/overview/overview.component';

export const OVERVIEW_ROUTES: Routes = [
  { path: '', component: OverviewComponent },
];

export default OVERVIEW_ROUTES;
